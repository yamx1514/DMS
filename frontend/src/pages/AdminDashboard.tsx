import React from "react";

const AdminDashboard: React.FC = () => (
  <div>
    <h1>Admin dashboard</h1>
    <p>Only administrators should see this page.</p>
  </div>
);

export default AdminDashboard;
